name: CI

on:
  push:
    branches: [ 1.0.x ]
  pull_request:
    branches: [ 1.0.x ]
  workflow_dispatch:

jobs:
  islandora-module-ci:
    uses: digitalutsc/reusable_workflows/.github/workflows/islandora-module-ci.yml@main
    with:
      module_name: json_field_processor
      install_chromedriver: true
